## Packages
framer-motion | Smooth page transitions and component animations
recharts | Visualizing task completion statistics
date-fns | Formatting dates for tasks and deadlines

## Notes
Auth is custom (session-based) via /api/login, /api/register
AI features (prioritize, summarize) are available via specific API endpoints
Theme should be dark with deep purple/blue gradients
